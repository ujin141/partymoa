/**
 * 홈에 띄우는 행사 팝업. **한 번에 하나만.**
 *
 * 슬러그만 적는다. 제목·날짜·장소·값은 DB 의 그 파티에서 그대로 읽는다 —
 * 여기 다시 적으면 파티를 고쳤을 때 팝업만 옛 값으로 남는다.
 *
 * 그 파티가 열려 있을 때(status open, 아직 안 끝남)만 뜬다. 끝나면
 * 저절로 사라지니 파티가 끝나고 이 줄을 지우러 오지 않아도 된다.
 * 다음 파티 때는 슬러그만 바꾼다.
 */
export const POPUP_SLUG = "after-moon-20260926";
